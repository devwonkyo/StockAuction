import 'package:flutter/material.dart';

class SelllistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('postlist Page')),
    );
  }
}
