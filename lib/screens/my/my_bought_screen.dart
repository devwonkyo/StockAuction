import 'package:flutter/material.dart';

class MyBoughtScreen extends StatelessWidget {
  const MyBoughtScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MySoldScreen')),
      body: const Center(child: Text('MySoldScreen')),
    );
  }
}
