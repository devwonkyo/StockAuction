import 'package:flutter/material.dart';

class MyDeliverScreen extends StatelessWidget {
  const MyDeliverScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MyDeliverScreen')),
      body: const Center(child: Text('MyDeliverScreen')),
    );
  }
}
