import 'package:flutter/material.dart';

class MyInfoUpdateScreen extends StatelessWidget {
  const MyInfoUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MyInfoUpdateScreen')),
      body: const Center(child: Text('MyInfoUpdateScreen')),
    );
  }
}
