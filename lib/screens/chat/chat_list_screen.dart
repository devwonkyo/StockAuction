import 'package:flutter/material.dart';

class ChatlistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('chatlist Page')),
    );
  }
}
