import 'package:flutter/material.dart';

class likePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('like list Page')),
    );
  }
}
